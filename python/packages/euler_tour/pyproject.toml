[project]
name = "euler-tour"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jingapore", email = "ngjingsong@gmail.com" }
]
requires-python = ">=3.14"
dependencies = ["utils"]

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
utils = {workspace = true}

[tool.basedpyright]
venvPath = "../../"
venv = ".venv"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
